package edu.cs3500.spreadsheets.provider;

import edu.cs3500.spreadsheets.model.IValue;

/**
 * this is a missing class in the given code,
 * create a value class which is contain a IValue.
 */
public class Value {
  IValue v;

  /**
   * contain our own IValue class for restore our data.
   *
   * @param v IValue
   */
  public Value(IValue v) {
    this.v = v;
  }
}
